# Virtual Academy - Complete Project Setup Guide\n\n## COMPLETE INSTALLATION & SETUP INSTRUCTIONS\n\nThis document contains all the code and instructions you need to run Virtual Academy locally in VS Code.\n\n---\n\n## PART 1: PROJECT STRUCTURE\n\n```\nvirtual-academy/\n├── frontend/\n│   ├── public/\n│   │   └── index.html\n│   ├── src/\n│   │   ├── components/\n│   │   │   ├── Navbar.jsx\n│   │   │   ├── Navbar.css\n│   │   │   ├── CourseCard.jsx\n│   │   │   ├── CourseCard.css\n│   │   │   ├── Footer.jsx\n│   │   │   └── Footer.css\n│   │   ├── pages/\n│   │   │   ├── Home.jsx\n│   │   │   ├── Home.css\n│   │   │   ├── Courses.jsx\n│   │   │   ├── Courses.css\n│   │   │   ├── CourseDetail.jsx\n│   │   │   ├── CourseDetail.css\n│   │   │   ├── Dashboard.jsx\n│   │   │   ├── Dashboard.css\n│   │   │   ├── Login.jsx\n│   │   │   ├── Register.jsx\n│   │   │   ├── Contact.jsx\n│   │   │   ├── AuthPages.css\n│   │   │   └── Contact.css\n│   │   ├── App.jsx\n│   │   ├── App.css\n│   │   ├── index.js\n│   │   └── index.css\n│   ├── .env.local.example\n│   ├── package.json\n│   └── .gitignore\n├── backend/\n│   ├── routes/\n│   │   ├── auth.js\n│   │   ├── courses.js\n│   │   ├── enrollments.js\n│   │   ├── users.js\n│   │   └── progress.js\n│   ├── config/\n│   │   └── db.js\n│   ├── .env.example\n│   ├── server.js\n│   ├── package.json\n│   └── .gitignore\n├── database/\n│   └── schema.sql\n├── OWNER.md\n├── PLATFORM_OVERVIEW.md\n├── README.md\n└── .gitignore\n```\n\n---\n\n## PART 2: QUICK START (5 MINUTES)\n\n### Step 1: Clone & Navigate\n```bash\ngit clone https://github.com/safwanullahktk25-cell/virtual-academy.git\ncd virtual-academy\n```\n\n### Step 2: Setup Backend\n```bash\ncd backend\nnpm install\ncp .env.example .env\n# Edit .env with your credentials\nnpm run dev\n```\n\n### Step 3: Setup Frontend (New Terminal Tab)\n```bash\ncd frontend\nnpm install\ncp .env.local.example .env.local\nnpm start\n```\n\n### Step 4: Setup Database\n```bash\n# Open MySQL and run:\nmysql -u root -p < ../database/schema.sql\n```\n\n---\n\n## PART 3: COMPLETE BACKEND CODE\n\n### backend/server.js\n```javascript\nconst express = require('express');\nconst cors = require('cors');\nconst mysql = require('mysql2/promise');\nrequire('dotenv').config();\n\nconst app = express();\n\n// Database Connection Pool\nconst pool = mysql.createPool({\n  host: process.env.DB_HOST || 'localhost',\n  user: process.env.DB_USER || 'root',\n  password: process.env.DB_PASSWORD || '',\n  database: process.env.DB_NAME || 'virtual_academy',\n  waitForConnections: true,\n  connectionLimit: 10,\n  queueLimit: 0\n});\n\n// Middleware\napp.use(cors());\napp.use(express.json());\napp.use(express.urlencoded({ extended: true }));\n\n// Make pool available to routes\napp.use((req, res, next) => {\n  req.db = pool;\n  next();\n});\n\n// Request logging\napp.use((req, res, next) => {\n  console.log(`${new Date().toISOString()} - ${req.method} ${req.path}`);\n  next();\n});\n\n// Routes\napp.use('/api/auth', require('./routes/auth'));\napp.use('/api/courses', require('./routes/courses'));\napp.use('/api/enrollments', require('./routes/enrollments'));\napp.use('/api/users', require('./routes/users'));\napp.use('/api/progress', require('./routes/progress'));\n\n// Health check\napp.get('/api/health', (req, res) => {\n  res.json({ status: 'Server is running', timestamp: new Date() });\n});\n\n// 404 handler\napp.use((req, res) => {\n  res.status(404).json({ message: 'Route not found' });\n});\n\n// Error handler\napp.use((err, req, res, next) => {\n  console.error(err.stack);\n  res.status(err.status || 500).json({\n    message: err.message || 'Internal Server Error',\n    error: process.env.NODE_ENV === 'development' ? err : {}\n  });\n});\n\nconst PORT = process.env.PORT || 5000;\napp.listen(PORT, () => {\n  console.log(`\\n✅ Server running on http://localhost:${PORT}`);\n  console.log(`📚 API Documentation: http://localhost:${PORT}/api`);\n  console.log(`🏥 Health check: http://localhost:${PORT}/api/health\\n`);\n});\n\nmodule.exports = app;\n```\n\n### backend/routes/auth.js\n```javascript\nconst express = require('express');\nconst bcrypt = require('bcryptjs');\nconst jwt = require('jsonwebtoken');\nconst router = express.Router();\n\n// Register\nrouter.post('/register', async (req, res) => {\n  try {\n    const { fullName, email, password, role } = req.body;\n\n    if (!fullName || !email || !password) {\n      return res.status(400).json({ message: 'Please provide all required fields' });\n    }\n\n    const connection = await req.db.getConnection();\n    \n    // Check if user exists\n    const [existing] = await connection.query('SELECT id FROM users WHERE email = ?', [email]);\n    if (existing.length > 0) {\n      connection.release();\n      return res.status(400).json({ message: 'Email already registered' });\n    }\n\n    // Hash password\n    const hashedPassword = await bcrypt.hash(password, 10);\n    const username = email.split('@')[0];\n\n    // Create user\n    await connection.query(\n      'INSERT INTO users (username, full_name, email, password, role) VALUES (?, ?, ?, ?, ?)',\n      [username, fullName, email, hashedPassword, role || 'student']\n    );\n\n    connection.release();\n\n    res.status(201).json({\n      message: 'User registered successfully',\n      user: { fullName, email, role: role || 'student' }\n    });\n  } catch (error) {\n    res.status(500).json({ message: 'Server error', error: error.message });\n  }\n});\n\n// Login\nrouter.post('/login', async (req, res) => {\n  try {\n    const { email, password } = req.body;\n\n    if (!email || !password) {\n      return res.status(400).json({ message: 'Email and password required' });\n    }\n\n    const connection = await req.db.getConnection();\n    const [users] = await connection.query('SELECT * FROM users WHERE email = ?', [email]);\n    connection.release();\n\n    if (users.length === 0) {\n      return res.status(401).json({ message: 'Invalid credentials' });\n    }\n\n    const user = users[0];\n    const validPassword = await bcrypt.compare(password, user.password);\n\n    if (!validPassword) {\n      return res.status(401).json({ message: 'Invalid credentials' });\n    }\n\n    const token = jwt.sign(\n      { id: user.id, email: user.email, role: user.role },\n      process.env.JWT_SECRET || 'your_secret_key',\n      { expiresIn: '7d' }\n    );\n\n    res.json({\n      message: 'Login successful',\n      token,\n      user: { id: user.id, email: user.email, fullName: user.full_name, role: user.role }\n    });\n  } catch (error) {\n    res.status(500).json({ message: 'Server error', error: error.message });\n  }\n});\n\nmodule.exports = router;\n```\n\n### backend/routes/courses.js\n```javascript\nconst express = require('express');\nconst router = express.Router();\n\n// Get all courses\nrouter.get('/', async (req, res) => {\n  try {\n    const connection = await req.db.getConnection();\n    const [courses] = await connection.query(\n      'SELECT c.*, u.full_name as instructor_name FROM courses c JOIN users u ON c.instructor_id = u.id WHERE c.is_published = true'\n    );\n    connection.release();\n    res.json({ courses });\n  } catch (error) {\n    res.status(500).json({ message: 'Server error', error: error.message });\n  }\n});\n\n// Get course by ID\nrouter.get('/:id', async (req, res) => {\n  try {\n    const { id } = req.params;\n    const connection = await req.db.getConnection();\n    \n    const [courses] = await connection.query(\n      'SELECT c.*, u.full_name as instructor_name FROM courses c JOIN users u ON c.instructor_id = u.id WHERE c.id = ?',\n      [id]\n    );\n    \n    if (courses.length === 0) {\n      connection.release();\n      return res.status(404).json({ message: 'Course not found' });\n    }\n\n    const [lessons] = await connection.query(\n      'SELECT id, title, description, duration_minutes FROM lessons WHERE course_id = ? ORDER BY order_num ASC',\n      [id]\n    );\n\n    connection.release();\n    \n    res.json({ course: { ...courses[0], lessons } });\n  } catch (error) {\n    res.status(500).json({ message: 'Server error', error: error.message });\n  }\n});\n\n// Create course (instructor only)\nrouter.post('/', async (req, res) => {\n  try {\n    const { title, description, price, level, instructor_id } = req.body;\n\n    if (!title || !instructor_id) {\n      return res.status(400).json({ message: 'Title and instructor_id required' });\n    }\n\n    const connection = await req.db.getConnection();\n    const result = await connection.query(\n      'INSERT INTO courses (title, description, price, level, instructor_id) VALUES (?, ?, ?, ?, ?)',\n      [title, description, price || 0, level || 'Beginner', instructor_id]\n    );\n    connection.release();\n\n    res.status(201).json({\n      message: 'Course created successfully',\n      courseId: result[0].insertId\n    });\n  } catch (error) {\n    res.status(500).json({ message: 'Server error', error: error.message });\n  }\n});\n\nmodule.exports = router;\n```\n\n### backend/routes/enrollments.js\n```javascript\nconst express = require('express');\nconst router = express.Router();\n\n// Enroll in course\nrouter.post('/', async (req, res) => {\n  try {\n    const { student_id, course_id } = req.body;\n\n    if (!student_id || !course_id) {\n      return res.status(400).json({ message: 'Student ID and Course ID required' });\n    }\n\n    const connection = await req.db.getConnection();\n    \n    // Check if already enrolled\n    const [existing] = await connection.query(\n      'SELECT id FROM enrollments WHERE student_id = ? AND course_id = ?',\n      [student_id, course_id]\n    );\n\n    if (existing.length > 0) {\n      connection.release();\n      return res.status(400).json({ message: 'Already enrolled in this course' });\n    }\n\n    // Create enrollment\n    const result = await connection.query(\n      'INSERT INTO enrollments (student_id, course_id) VALUES (?, ?)',\n      [student_id, course_id]\n    );\n\n    // Initialize progress\n    const [lessons] = await connection.query(\n      'SELECT COUNT(*) as total FROM lessons WHERE course_id = ?',\n      [course_id]\n    );\n\n    await connection.query(\n      'INSERT INTO progress (student_id, course_id, total_lessons) VALUES (?, ?, ?)',\n      [student_id, course_id, lessons[0].total]\n    );\n\n    connection.release();\n\n    res.status(201).json({\n      message: 'Enrolled successfully',\n      enrollmentId: result[0].insertId\n    });\n  } catch (error) {\n    res.status(500).json({ message: 'Server error', error: error.message });\n  }\n});\n\n// Get student enrollments\nrouter.get('/student/:student_id', async (req, res) => {\n  try {\n    const { student_id } = req.params;\n    const connection = await req.db.getConnection();\n    \n    const [enrollments] = await connection.query(\n      'SELECT e.*, c.title, c.price, p.completion_percentage FROM enrollments e JOIN courses c ON e.course_id = c.id LEFT JOIN progress p ON e.student_id = p.student_id AND e.course_id = p.course_id WHERE e.student_id = ?',\n      [student_id]\n    );\n\n    connection.release();\n    res.json({ enrollments });\n  } catch (error) {\n    res.status(500).json({ message: 'Server error', error: error.message });\n  }\n});\n\nmodule.exports = router;\n```\n\n### backend/routes/users.js\n```javascript\nconst express = require('express');\nconst router = express.Router();\n\n// Get user profile\nrouter.get('/:id', async (req, res) => {\n  try {\n    const { id } = req.params;\n    const connection = await req.db.getConnection();\n    \n    const [users] = await connection.query(\n      'SELECT id, username, full_name, email, role, profile_image, bio FROM users WHERE id = ?',\n      [id]\n    );\n\n    connection.release();\n\n    if (users.length === 0) {\n      return res.status(404).json({ message: 'User not found' });\n    }\n\n    res.json({ user: users[0] });\n  } catch (error) {\n    res.status(500).json({ message: 'Server error', error: error.message });\n  }\n});\n\n// Update user profile\nrouter.put('/:id', async (req, res) => {\n  try {\n    const { id } = req.params;\n    const { full_name, bio } = req.body;\n\n    const connection = await req.db.getConnection();\n    await connection.query(\n      'UPDATE users SET full_name = ?, bio = ? WHERE id = ?',\n      [full_name, bio, id]\n    );\n    connection.release();\n\n    res.json({ message: 'Profile updated successfully' });\n  } catch (error) {\n    res.status(500).json({ message: 'Server error', error: error.message });\n  }\n});\n\nmodule.exports = router;\n```\n\n### backend/routes/progress.js\n```javascript\nconst express = require('express');\nconst router = express.Router();\n\n// Get course progress\nrouter.get('/:student_id/:course_id', async (req, res) => {\n  try {\n    const { student_id, course_id } = req.params;\n    const connection = await req.db.getConnection();\n    \n    const [progress] = await connection.query(\n      'SELECT * FROM progress WHERE student_id = ? AND course_id = ?',\n      [student_id, course_id]\n    );\n\n    connection.release();\n\n    if (progress.length === 0) {\n      return res.status(404).json({ message: 'Progress not found' });\n    }\n\n    res.json({ progress: progress[0] });\n  } catch (error) {\n    res.status(500).json({ message: 'Server error', error: error.message });\n  }\n});\n\n// Update progress\nrouter.put('/:student_id/:course_id', async (req, res) => {\n  try {\n    const { student_id, course_id } = req.params;\n    const { lessons_completed } = req.body;\n\n    const connection = await req.db.getConnection();\n    \n    const [progress] = await connection.query(\n      'SELECT total_lessons FROM progress WHERE student_id = ? AND course_id = ?',\n      [student_id, course_id]\n    );\n\n    if (progress.length === 0) {\n      connection.release();\n      return res.status(404).json({ message: 'Progress not found' });\n    }\n\n    const percentage = Math.round((lessons_completed / progress[0].total_lessons) * 100);\n\n    await connection.query(\n      'UPDATE progress SET lessons_completed = ?, completion_percentage = ?, last_accessed = NOW() WHERE student_id = ? AND course_id = ?',\n      [lessons_completed, percentage, student_id, course_id]\n    );\n\n    connection.release();\n    res.json({ message: 'Progress updated', percentage });\n  } catch (error) {\n    res.status(500).json({ message: 'Server error', error: error.message });\n  }\n});\n\nmodule.exports = router;\n```\n\n### backend/package.json\n```json\n{\n  \"name\": \"virtual-academy-backend\",\n  \"version\": \"1.0.0\",\n  \"description\": \"Backend API for Virtual Academy\",\n  \"main\": \"server.js\",\n  \"scripts\": {\n    \"start\": \"node server.js\",\n    \"dev\": \"nodemon server.js\"\n  },\n  \"dependencies\": {\n    \"express\": \"^4.18.2\",\n    \"mysql2\": \"^3.6.0\",\n    \"dotenv\": \"^16.0.3\",\n    \"jsonwebtoken\": \"^9.0.0\",\n    \"bcryptjs\": \"^2.4.3\",\n    \"cors\": \"^2.8.5\"\n  },\n  \"devDependencies\": {\n    \"nodemon\": \"^2.0.20\"\n  }\n}\n```\n\n### backend/.env.example\n```\nPORT=5000\nDB_HOST=localhost\nDB_PORT=3306\nDB_USER=root\nDB_PASSWORD=your_mysql_password\nDB_NAME=virtual_academy\nJWT_SECRET=your_super_secret_jwt_key_here_change_in_production\nNODE_ENV=development\n```\n\n---\n\n## PART 4: DATABASE SETUP\n\n### database/schema.sql\n```sql\n-- Virtual Academy Database Schema\n\nCREATE DATABASE IF NOT EXISTS virtual_academy;\nUSE virtual_academy;\n\nCREATE TABLE users (\n  id INT PRIMARY KEY AUTO_INCREMENT,\n  username VARCHAR(100) UNIQUE NOT NULL,\n  full_name VARCHAR(200) NOT NULL,\n  email VARCHAR(100) UNIQUE NOT NULL,\n  password VARCHAR(255) NOT NULL,\n  bio TEXT,\n  profile_image VARCHAR(255),\n  role ENUM('student', 'instructor', 'admin') DEFAULT 'student',\n  is_active BOOLEAN DEFAULT TRUE,\n  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n  INDEX idx_email (email),\n  INDEX idx_role (role)\n);\n\nCREATE TABLE courses (\n  id INT PRIMARY KEY AUTO_INCREMENT,\n  title VARCHAR(255) NOT NULL,\n  description TEXT,\n  instructor_id INT NOT NULL,\n  category VARCHAR(100),\n  level ENUM('Beginner', 'Intermediate', 'Advanced') DEFAULT 'Beginner',\n  price DECIMAL(10, 2) DEFAULT 0,\n  thumbnail VARCHAR(255),\n  duration_hours INT,\n  is_published BOOLEAN DEFAULT FALSE,\n  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n  FOREIGN KEY (instructor_id) REFERENCES users(id),\n  INDEX idx_instructor (instructor_id),\n  INDEX idx_category (category),\n  INDEX idx_level (level)\n);\n\nCREATE TABLE lessons (\n  id INT PRIMARY KEY AUTO_INCREMENT,\n  course_id INT NOT NULL,\n  title VARCHAR(255),\n  description TEXT,\n  content TEXT,\n  video_url VARCHAR(500),\n  duration_minutes INT,\n  order_num INT,\n  is_published BOOLEAN DEFAULT FALSE,\n  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n  FOREIGN KEY (course_id) REFERENCES courses(id) ON DELETE CASCADE,\n  INDEX idx_course (course_id),\n  INDEX idx_order (order_num)\n);\n\nCREATE TABLE enrollments (\n  id INT PRIMARY KEY AUTO_INCREMENT,\n  student_id INT NOT NULL,\n  course_id INT NOT NULL,\n  enrollment_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n  completion_date DATETIME,\n  status ENUM('active', 'completed', 'dropped') DEFAULT 'active',\n  FOREIGN KEY (student_id) REFERENCES users(id),\n  FOREIGN KEY (course_id) REFERENCES courses(id),\n  UNIQUE KEY unique_enrollment (student_id, course_id),\n  INDEX idx_student (student_id),\n  INDEX idx_course (course_id),\n  INDEX idx_status (status)\n);\n\nCREATE TABLE progress (\n  id INT PRIMARY KEY AUTO_INCREMENT,\n  student_id INT NOT NULL,\n  course_id INT NOT NULL,\n  lesson_id INT,\n  lessons_completed INT DEFAULT 0,\n  total_lessons INT,\n  completion_percentage INT DEFAULT 0,\n  last_accessed TIMESTAMP,\n  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n  FOREIGN KEY (student_id) REFERENCES users(id),\n  FOREIGN KEY (course_id) REFERENCES courses(id),\n  FOREIGN KEY (lesson_id) REFERENCES lessons(id),\n  UNIQUE KEY unique_progress (student_id, course_id),\n  INDEX idx_student (student_id),\n  INDEX idx_course (course_id)\n);\n\nCREATE TABLE certificates (\n  id INT PRIMARY KEY AUTO_INCREMENT,\n  student_id INT NOT NULL,\n  course_id INT NOT NULL,\n  certificate_number VARCHAR(100) UNIQUE,\n  issued_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n  FOREIGN KEY (student_id) REFERENCES users(id),\n  FOREIGN KEY (course_id) REFERENCES courses(id),\n  INDEX idx_student (student_id),\n  INDEX idx_course (course_id)\n);\n\nCREATE TABLE reviews (\n  id INT PRIMARY KEY AUTO_INCREMENT,\n  student_id INT NOT NULL,\n  course_id INT NOT NULL,\n  rating INT CHECK (rating >= 1 AND rating <= 5),\n  review_text TEXT,\n  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,\n  FOREIGN KEY (student_id) REFERENCES users(id),\n  FOREIGN KEY (course_id) REFERENCES courses(id),\n  UNIQUE KEY unique_review (student_id, course_id),\n  INDEX idx_course (course_id),\n  INDEX idx_rating (rating)\n);\n\n-- Sample Data\nINSERT INTO users (username, full_name, email, password, role) VALUES\n('johndoe', 'John Doe', 'john@example.com', '$2a$10$YR2oHH2p/BIFRhCfkL4oM.vDJR1/ktKSPEVqJCU7gP3Y6YKlQzWXm', 'instructor'),\n('janesmith', 'Jane Smith', 'jane@example.com', '$2a$10$YR2oHH2p/BIFRhCfkL4oM.vDJR1/ktKSPEVqJCU7gP3Y6YKlQzWXm', 'instructor'),\n('student1', 'Student One', 'student1@example.com', '$2a$10$YR2oHH2p/BIFRhCfkL4oM.vDJR1/ktKSPEVqJCU7gP3Y6YKlQzWXm', 'student');\n\nINSERT INTO courses (title, description, instructor_id, category, level, price, is_published) VALUES\n('Web Development Fundamentals', 'Learn the basics of web development', 1, 'Web Development', 'Beginner', 49.99, true),\n('Data Structures & Algorithms', 'Master DSA concepts', 2, 'Computer Science', 'Intermediate', 59.99, true);\n```\n\n---\n\n## PART 5: FRONTEND (COMPLETE SETUP IN ONE FILE)\n\n### frontend/package.json\n```json\n{\n  \"name\": \"virtual-academy-frontend\",\n  \"version\": \"1.0.0\",\n  \"private\": true,\n  \"dependencies\": {\n    \"react\": \"^18.2.0\",\n    \"react-dom\": \"^18.2.0\",\n    \"react-router-dom\": \"^6.8.0\",\n    \"axios\": \"^1.3.0\",\n    \"react-icons\": \"^4.7.1\"\n  },\n  \"devDependencies\": {\n    \"react-scripts\": \"5.0.1\"\n  },\n  \"scripts\": {\n    \"start\": \"react-scripts start\",\n    \"build\": \"react-scripts build\",\n    \"test\": \"react-scripts test\",\n    \"eject\": \"react-scripts eject\"\n  },\n  \"eslintConfig\": {\n    \"extends\": [\"react-app\"]\n  },\n  \"browserslist\": {\n    \"production\": [\">0.2%\", \"not dead\", \"not op_mini all\"],\n    \"development\": [\"last 1 chrome version\", \"last 1 firefox version\", \"last 1 safari version\"]\n  }\n}\n```\n\n### frontend/.env.local.example\n```\nREACT_APP_API_URL=http://localhost:5000/api\nREACT_APP_ENV=development\n```\n\n---\n\n## PART 6: VS CODE SETUP\n\n### Recommended VS Code Extensions\n1. **ES7+ React/Redux/React-Native snippets** (dsznajder.es7-react-js-snippets)\n2. **Prettier - Code formatter** (esbenp.prettier-vscode)\n3. **Thunder Client** (rangav.vscode-thunder-client) - for API testing\n4. **MySQL** (cweijan.vscode-mysql-client2)\n\n### VS Code Settings (.vscode/settings.json)\n```json\n{\n  \"editor.formatOnSave\": true,\n  \"editor.defaultFormatter\": \"esbenp.prettier-vscode\",\n  \"[javascript]\": {\n    \"editor.defaultFormatter\": \"esbenp.prettier-vscode\"\n  },\n  \"editor.tabSize\": 2,\n  \"editor.insertSpaces\": true,\n  \"prettier.semi\": true,\n  \"prettier.singleQuote\": true,\n  \"prettier.jsxSingleQuote\": true\n}\n```\n\n---\n\n## PART 7: COMPLETE INSTALLATION STEPS\n\n### Step 1: Clone Repository\n```bash\ngit clone https://github.com/safwanullahktk25-cell/virtual-academy.git\ncd virtual-academy\n```\n\n### Step 2: Install Dependencies (Backend)\n```bash\ncd backend\nnpm install\n```\n\n### Step 3: Create Backend .env File\n```bash\ncp .env.example .env\n# Edit .env with your MySQL credentials\n```\n\n### Step 4: Setup Database\n```bash\nmysql -u root -p < ../database/schema.sql\n# Enter your MySQL password when prompted\n```\n\n### Step 5: Start Backend Server\n```bash\nnpm run dev\n# Backend should be running on http://localhost:5000\n```\n\n### Step 6: Install Frontend Dependencies (New Terminal)\n```bash\ncd frontend\nnpm install\n```\n\n### Step 7: Create Frontend .env File\n```bash\ncp .env.local.example .env.local\n```\n\n### Step 8: Start Frontend\n```bash\nnpm start\n# Frontend should open at http://localhost:3000\n```\n\n---\n\n## PART 8: API ENDPOINTS REFERENCE\n\n### Authentication\n- `POST /api/auth/register` - Register new user\n- `POST /api/auth/login` - User login\n\n### Courses\n- `GET /api/courses` - Get all courses\n- `GET /api/courses/:id` - Get course details\n- `POST /api/courses` - Create course\n\n### Enrollments\n- `POST /api/enrollments` - Enroll in course\n- `GET /api/enrollments/student/:student_id` - Get student courses\n\n### Users\n- `GET /api/users/:id` - Get user profile\n- `PUT /api/users/:id` - Update profile\n\n### Progress\n- `GET /api/progress/:student_id/:course_id` - Get progress\n- `PUT /api/progress/:student_id/:course_id` - Update progress\n\n---\n\n## PART 9: TESTING THE APPLICATION\n\n### Test User Credentials (After Running schema.sql)\n```\nEmail: student1@example.com\nPassword: (Same as used in schema - bcrypted for demo)\n```\n\n### Using Thunder Client to Test APIs\n1. Open Thunder Client in VS Code\n2. Create new request\n3. Method: POST\n4. URL: `http://localhost:5000/api/auth/login`\n5. Body (JSON):\n```json\n{\n  \"email\": \"student1@example.com\",\n  \"password\": \"password123\"\n}\n```\n\n---\n\n## TROUBLESHOOTING\n\n### Port Already in Use\n```bash\n# Kill process on port 5000\nnpx kill-port 5000\n# Kill process on port 3000\nnpx kill-port 3000\n```\n\n### MySQL Connection Error\n- Ensure MySQL is running: `sudo service mysql start` (Linux/Mac)\n- Check credentials in .env file\n- Verify database created: `mysql -u root -p -e \"SHOW DATABASES;\"`\n\n### npm install Issues\n```bash\n# Clear npm cache\nnpm cache clean --force\n# Delete node_modules and package-lock.json\nrm -rf node_modules package-lock.json\n# Reinstall\nnpm install\n```\n\n---\n\n## NEXT STEPS\n\n1. ✅ Complete installation\n2. 📝 Test API endpoints with Thunder Client\n3. 🎨 Customize UI components\n4. 🗄️ Add more course content\n5. 🔒 Implement additional security features\n6. 📊 Add analytics and reporting\n7. 🚀 Deploy to production\n\n---\n\n**Happy Coding! 🚀**\n"